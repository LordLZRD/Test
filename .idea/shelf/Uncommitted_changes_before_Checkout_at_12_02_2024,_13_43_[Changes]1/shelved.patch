Index: Saloon/models.py
===================================================================
diff --git a/Saloon/models.py b/Saloon/models.py
new file mode 100644
--- /dev/null	
+++ b/Saloon/models.py	
@@ -0,0 +1,148 @@
+from django.db import models
+from django_countries.fields import CountryField
+
+
+class Saloon(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    name = models.CharField(max_length=255)
+    country = CountryField()
+    balance = models.DecimalField(max_digits=10, decimal_places=4)
+    #  characteristics = models.JSONField()
+
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.name
+
+
+class Characteristics(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    class_of_auto = models.CharField(max_length=1)  # BAR
+    year = models.IntegerField()
+    motor = models.CharField(max_length=10)  # BAR
+
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.id  # Is correct?
+
+
+class Stock(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    name = models.CharField(max_length=255)
+    description = models.TextField()
+    begin = models.DateField()
+    end = models.DateField()
+    saloon_id = models.ForeignKey('Saloon', on_delete=models.PROTECT, null=True)
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.name
+
+
+class AutoList(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    serial_number = models.CharField(max_length=40)
+    auto_id = models.ForeignKey('Auto', on_delete=models.PROTECT, null=True)
+
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.serial_number
+
+
+class Auto(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    make = models.CharField(max_length=25)
+    class_of_auto = models.CharField(max_length=1)  # BAR
+    model = models.CharField(max_length=25)
+    year = models.IntegerField()
+    motor = models.CharField(max_length=10)  # BAR
+
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.id  # Is correct?
+
+
+class SaloonHistory(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    date = models.DateField()
+    auto_list_id = models.ForeignKey('AutoList', on_delete=models.PROTECT, null=True)
+    consumer_id = models.ForeignKey('Consumer', on_delete=models.PROTECT, null=True)
+    saloon_id = models.ForeignKey('Saloon', on_delete=models.PROTECT, null=True)
+    characteristics_id = models.ForeignKey('Characteristics', on_delete=models.PROTECT, null=True)
+
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.id  # Is correct?
+
+
+class Offer(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    auto_id = models.ForeignKey('Auto', on_delete=models)  # How to choose a car?
+    max_money_offer = models.DecimalField()
+    consumer_id = models.ForeignKey('Consumer', on_delete=models.PROTECT, null=True)
+
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.id  # Is correct?
+
+
+class Consumer(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    name = models.CharField(max_length=255)
+    balance = models.DecimalField(max_digits=10, decimal_places=4)
+    number_of_purchases = models.IntegerField()
+
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.name
+
+
+class ConsumerHistory(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    date = models.DateField()
+    auto_list_id = models.ForeignKey('AutoList', on_delete=models.PROTECT, null=True)
+    consumer_id = models.ForeignKey('Consumer', on_delete=models.PROTECT, null=True)
+    saloon_id = models.ForeignKey('Saloon', on_delete=models.PROTECT, null=True)
+
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.id  # Is correct?
+
+
+class Dealer(models.Model):
+    id = models.BigAutoField(primary_key=True)
+    name = models.CharField(max_length=255)
+    year_of_foundation = models.IntegerField()
+    amount_of_consumers = models.IntegerField()
+
+    is_active = models.BooleanField()
+    create_time = models.DateTimeField()
+    update_time = models.DateTimeField()
+
+    def __str__(self):
+        return self.name
diff --git a/Saloon/__init__.py b/Saloon/__init__.py
new file mode 100644
